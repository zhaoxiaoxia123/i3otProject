import { DatePickerBase } from '../common/bs-date-picker-base.class';
import { BsDatePickerState } from '../common/bs-date-picker-state.provider';
import { BsDatePickerOptions } from '../common/bs-date-picker-options.provider';
import * as moment from 'moment';
export declare class BsCustomRangePickerComponent extends DatePickerBase {
    isShown: boolean;
    isCustomRangeShown: boolean;
    ranges: {
        key: string;
        value: moment.Moment[];
    }[];
    private _showCalendars;
    private _prevSelected;
    constructor(datePickerService: BsDatePickerState, options: BsDatePickerOptions);
    active(start: any, end: any): void;
    refresh(): void;
    selectRange(range: moment.Moment[]): void;
    previewRange(range: moment.Moment[]): void;
    finishPreviewRange(): void;
    showCalendars(): void;
}
