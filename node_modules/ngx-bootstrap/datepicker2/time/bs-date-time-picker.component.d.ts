import { EventEmitter } from '@angular/core';
import { DatePickerBase } from '../common/bs-date-picker-base.class';
import { BsDatePickerState } from '../common/bs-date-picker-state.provider';
import { BsDatePickerOptions } from '../common/bs-date-picker-options.provider';
import * as moment from 'moment';
export declare class BsDateTimePickerComponent extends DatePickerBase {
    date: moment.Moment;
    dateChange: EventEmitter<moment.Moment>;
    hours: string;
    minutes: string;
    ampm: string;
    showAmPm: boolean;
    constructor(datePickerService: BsDatePickerState, options: BsDatePickerOptions);
    refresh(): void;
    add(granularity: string): void;
    subtract(granularity: string): void;
    setTime(date: moment.Moment): moment.Moment;
    toggleMeridiem(): void;
}
