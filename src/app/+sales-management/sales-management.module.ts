import { NgModule } from '@angular/core';
import {routing} from "./sales-management-routing.module";

@NgModule({
  imports: [
      routing,
  ],
  declarations: []
})
export class SalesManagementModule { }
